# Асинхронний сортувальник файлів

Цей проєкт реалізує **асинхронний Python-скрипт**, який читає всі файли у вказаній вихідній папці та розподіляє їх по підпапках у директорії призначення на основі **розширення файлів**.

## 🚀 Функціональність
✅ Асинхронне читання файлів  
✅ Асинхронне копіювання через `aiofiles`  
✅ Можливість **переміщення** файлів за допомогою `--move`  
✅ Відображення **прогресу** в консолі  
✅ Логування дій у `logs/sorter.log`  
✅ Логування **часу виконання**  
✅ Обробка аргументів командного рядка  
✅ Покриття тестами  

---

## 📂 Структура проєкту

```
c:\Projects\goit-pythonweb-hw-04\
│
├── sorter\                   # Основний модуль сортування
│   ├── __init__.py
│   ├── sorter.py             # Головний модуль сортування
│   ├── file_reader.py        # Читання файлів у папці
│   ├── file_copier.py        # Копіювання файлів у відповідні папки
│   ├── logger.py             # Логування дій
│
├── tests\                    # Тести
│   ├── __init__.py
│   ├── test_sorter.py        # Тести основного модуля
│   ├── test_file_reader.py   # Тести функції читання файлів
│   ├── test_file_copier.py   # Тести функції копіювання файлів
│
├── logs\                     # Логи роботи програми
│   ├── sorter.log
│
├── .gitignore                # Ігнорує непотрібні файли у Git
├── requirements.txt          # Необхідні бібліотеки
├── README.md                 # Цей файл
├── main.py                   # Головний скрипт для запуску програми
```

---

## 🛠 Встановлення та запуск

### 1️⃣ **Клонування репозиторію**
```sh
git clone https://github.com/твій-юзернейм/goit-pythonweb-hw-04.git
cd goit-pythonweb-hw-04
```

### 2️⃣ **Створення віртуального середовища**
```sh
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate      # Windows
```

### 3️⃣ **Встановлення залежностей**
```sh
pip install -r requirements.txt
```

---

## ▶️ Запуск сортувальника
Програма приймає такі аргументи:
- `source` – шлях до вихідної папки  
- `destination` – шлях до цільової папки  
- `--move` – **опціональний** параметр для переміщення файлів (замість копіювання)

```sh
python main.py source sorted --move
```

📌 **Приклад структури після сортування:**
```
sorted/
 ├── pdf/
 │   ├── document.pdf
 ├── jpg/
 │   ├── photo.jpg
 ├── txt/
 │   ├── notes.txt
```

---

## ⏳ Логування часу виконання
Після завершення роботи виводиться:
```
Сортуємо файли з source у sorted...
(1/3) Обробляємо document.pdf...
(2/3) Обробляємо photo.jpg...
(3/3) Обробляємо notes.txt...
⏳ Час виконання: 1.45 секунд
Сортування завершено!
```

---

## 🧪 Запуск тестів
```sh
pytest tests/
```

---

## 📜 Ліцензія
Проєкт створений для навчальних цілей. Використовуйте вільно! 🚀
